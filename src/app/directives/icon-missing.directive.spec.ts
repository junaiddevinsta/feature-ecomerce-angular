import { IconMissingDirective } from './icon-missing.directive';

describe('IconMissingDirective', () => {
  it('should create an instance', () => {
    const directive = new IconMissingDirective();
    expect(directive).toBeTruthy();
  });
});
